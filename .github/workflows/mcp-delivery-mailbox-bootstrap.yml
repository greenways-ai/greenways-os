name: Durable MCP delivery mailbox bootstrap

on:
  push:
    branches:
      - agent/mcp-durable-route-mailbox-33

permissions:
  contents: write

jobs:
  bootstrap:
    if: github.actor != 'github-actions[bot]' && github.event.head_commit.message == 'Run durable MCP delivery mailbox bootstrap'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: services/mcp-gateway/package-lock.json

      - name: Finish delivery mailbox integration
        run: |
          python .github/agent/mcp-delivery-mailbox/finish.py
          python .github/agent/mcp-delivery-mailbox/clock.py

      - name: Install MCP gateway dependencies
        working-directory: services/mcp-gateway
        run: npm ci

      - name: Run delivery, SQLite, and Worker checks
        working-directory: services/mcp-gateway
        run: npm run check

      - name: Verify repository patch
        run: git diff --check

      - name: Remove bootstrap files
        run: |
          rm -rf .github/agent/mcp-delivery-mailbox
          rm -f .github/workflows/mcp-delivery-mailbox-bootstrap.yml

      - name: Commit and push
        run: |
          git config user.name "ChatGPT Codex Connector"
          git config user.email "chatgpt-codex-connector@users.noreply.github.com"
          git add -A
          git commit -m "Add durable MCP route mailbox"
          git push origin HEAD:${GITHUB_REF_NAME}
