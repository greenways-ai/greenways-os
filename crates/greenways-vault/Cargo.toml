[package]
name = "greenways-vault"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
greenways-provider = { path = "../greenways-provider" }
greenways-protocol = { path = "../greenways-protocol" }
keyring = { version = "=3.6.3", features = ["apple-native", "windows-native", "sync-secret-service", "vendored"] }
serde.workspace = true
serde_json.workspace = true
ureq = { version = "=2.12.1", features = ["json"] }
# Keep ureq on the repository Rust 1.85-compatible URL/IDNA line.
url = "=2.5.2"
zeroize = "1.9.0"

[lints]
workspace = true
